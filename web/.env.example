# Base URL of the Axis CRM HTTP API. Same-origin (/api) works in dev via the
# Vite proxy; set an absolute URL when the panel is served from another origin
# (which must be listed in the backend PANEL_ORIGIN allowlist).
# No secrets here — Vite exposes these variables to the browser.
VITE_API_URL=
